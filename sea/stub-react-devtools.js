// Stub for ink's optional `react-devtools-core` import. Ink only touches it when
// DEV=true; the warmup never runs in that mode, so an empty default keeps the
// static import binding satisfiable without pulling the real dependency into the bundle.
export default {};
