{
  "name": "agent-warmup",
  "version": "1.2.0",
  "description": "Prime AI coding-agent usage windows from one terminal scheduler.",
  "keywords": [
    "claude",
    "claude-code",
    "cli",
    "codex",
    "cron",
    "glm",
    "kimi",
    "launchd",
    "macos",
    "minimax",
    "scheduler",
    "tmux",
    "usage-limit",
    "warmup"
  ],
  "homepage": "https://github.com/d0whc3r/agent-warmup#readme",
  "bugs": {
    "url": "https://github.com/d0whc3r/agent-warmup/issues"
  },
  "license": "MIT",
  "author": "d0whc3r",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/d0whc3r/agent-warmup.git"
  },
  "bin": {
    "agent-warmup": "dist/agent-warmup.mjs"
  },
  "files": [
    "dist",
    "src/assets/arm-claude.sh",
    "src/assets/arm-codex.sh",
    "src/assets/arm-kimi.sh",
    "src/assets/arm-opencode.sh"
  ],
  "os": [
    "darwin",
    "linux"
  ],
  "type": "module",
  "scripts": {
    "dev": "tsx src/cli.ts",
    "start": "tsx src/cli.ts",
    "build": "tsdown",
    "build:sea": "bash scripts/build-sea.sh",
    "prepare": "tsdown && (husky || true)",
    "typecheck": "tsc --noEmit",
    "test": "tsdown && node --import tsx --test \"test/*.test.ts\"",
    "test:coverage": "tsdown && node --import tsx --test --experimental-test-coverage --test-coverage-exclude=\"dist/**\" --test-coverage-exclude=\"node_modules/**\" --test-coverage-exclude=\"test/**\" --test-coverage-lines=97 --test-coverage-functions=94 --test-coverage-branches=85 \"test/*.test.ts\"",
    "coverage:lcov": "tsdown && mkdir -p coverage && node --import tsx --test --experimental-test-coverage --test-coverage-exclude=\"dist/**\" --test-coverage-exclude=\"node_modules/**\" --test-coverage-exclude=\"test/**\" --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout \"test/*.test.ts\"",
    "lint": "oxlint --no-error-on-unmatched-pattern",
    "lint:fix": "oxlint --no-error-on-unmatched-pattern --fix --fix-suggestions",
    "format": "oxfmt --no-error-on-unmatched-pattern",
    "format:check": "oxfmt --no-error-on-unmatched-pattern --check",
    "format:fix": "pnpm run lint:fix && pnpm run format",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run format:check && pnpm test"
  },
  "dependencies": {
    "date-fns": "4.4.0",
    "ink": "7.1.1",
    "react": "19.2.8"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@semantic-release/exec": "7.1.0",
    "@semantic-release/git": "11.0.1",
    "@types/node": "26.5.1",
    "@types/react": "19.2.18",
    "husky": "9.1.7",
    "knip": "6.34.0",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "semantic-release": "25.0.9",
    "tsdown": "0.23.0",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@12.3.4"
}
